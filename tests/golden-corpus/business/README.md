# business/ — 业务基线（待宿主提供）

业务基线用例用于区分「库支持」与「业务已用」（spec §G.75）。**当前为空，是有意为之。**

纳入条件：

1. 由宿主提供**脱敏**后的真实模板、数据与旧输出；
2. **禁止包含任何真实个人信息**（姓名、证件号、电话、住址、病历等）；脱敏数据须无法回溯到真实个人；
3. 每个用例遵循与库级基线相同的格式：`case.json`（`tier: "business"`）+ `data.json` + `expected/semantics.json`，并说明脱敏方式；
4. 应包含真实长表格与 50 页文档各至少一份（spec 测试决策表）。

在宿主提供前，本目录只含本 README；加载器测试断言此处为空，防止误入未脱敏内容。
