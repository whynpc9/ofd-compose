# 32: 安全加固与负向语料

**What to build:** 平台面对恶意或错误输入时返回可辨识结果而不是崩溃、耗尽资源或静默降级：错误 ZIP/XML、外部实体、路径穿越、远程 URL、超大字体/图片、超长数组、重复键、超高表格行、缺字、未知模型扩展、未收敛页数域、取消中的作业、过期预览覆盖新修订。SBOM 与许可检查成为发布门禁；日志脱敏经测试验证。

**Blocked by:** 21 .NET API + Job Host

**Status:** ready-for-agent

- [ ] XML：DTD 禁用、无外部解析器；ZIP：条目数/大小/路径白名单；字体/图片字节预算；JSON 深度/大小限制
- [ ] 远程 URL 与任意文件路径在所有入口被拒绝（`RESOURCE_FORBIDDEN`）
- [ ] spec §7.3 全部负向场景各有测试并断言诊断 code
- [ ] 作业取消中止 Worker 且不留下 spool 垃圾；超时 kill 后池恢复
- [ ] 日志脱敏测试：注入敏感值后日志中不出现
- [ ] CI：CycloneDX SBOM（npm + .NET）、许可白名单、依赖漏洞扫描作为门禁
- [ ] 简单模糊测试（畸形 IR/模型/ZIP）跑一定迭代无崩溃
