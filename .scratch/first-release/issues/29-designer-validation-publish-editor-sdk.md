# 29: 工作台：校验面板、版本化发布、Editor SDK

**What to build:** 模板作者在保存/发布前运行校验，得到未绑定字段、不支持函数/格式、作用域歧义、字体缺失、表格越界、条码参数错误等节点级诊断并点击定位；发布生成版本化模板产物与兼容声明（模型版本、表达式语言版本、绑定策略版本、字体与资源锁）。宿主可通过框架无关的 Editor SDK 嵌入工作台，接收校验事件、source-map 事件与渲染配置回调。

**Blocked by:** 26 工作台：结构块, 28 工作台：媒体配置

**Status:** ready-for-agent

- [ ] 校验面板：运行共享内核的编译+绑定（样例数据）+排版检查，诊断列表可点击定位到节点
- [ ] 发布：版本化产物 + 兼容声明；发布被校验错误阻止
- [ ] Editor SDK：加载/导出模型、设计/只读模式、schema 适配、资源注入、校验事件、source-map 事件、渲染配置；销毁清理
- [ ] SDK 以 Web 组件或命令式 API 暴露，React 仅在 designer 壳；有一个非 React 宿主页面的嵌入示例
- [ ] 宿主不接触 canvas-editor 私有结构（API 表面测试）
- [ ] 打印预览模式，不产生未记录的内容转换
